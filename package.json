{
  "name": "util.constants",
  "version": "0.0.10",
  "author": "jmquigley <jmquigley@outlook.com>",
  "description": "General programming constants and regex strings",
  "main": "./index.js",
  "types": "./index.d.ts",
  "private": false,
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/jmquigley/util.constants"
  },
  "bugs": {
    "url": "https://github.com/jmquigley/util.constants/issues"
  },
  "engines": {
    "node": ">=7.x"
  },
  "devDependencies": {
    "@types/node": "^10.12.18",
    "ava": "^1.0.1",
    "coveralls": "^3.0.2",
    "debug": "^4.1.1",
    "nyc": "^13.1.0",
    "tslint": "^5.12.0",
    "typescript": "^3.2.2",
    "util.pkgwrap": "^0.0.93"
  },
  "pkgwrap": {
    "cleanup": [
      "index.d.ts",
      "index.js",
      "test.d.ts",
      "test.js"
    ]
  },
  "scripts": {
    "all": "yarn install && yarn run build && yarn test && yarn run report",
    "build": "yarn run clean && yarn run lint && pkgwrap --build",
    "clean": "pkgwrap --clean",
    "coverage": "pkgwrap --coverage",
    "lint": "pkgwrap --lint",
    "prepublishOnly": "yarn run all",
    "report": "pkgwrap --reporting",
    "test": "pkgwrap --testing --ava"
  }
}
