{
	"name": "util.constants",
	"version": "0.0.20",
	"author": "jmquigley <jmquigley@outlook.com>",
	"description": "General programming constants and regex strings",
	"private": false,
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "https://github.com/jmquigley/util.constants"
	},
	"bugs": {
		"url": "https://github.com/jmquigley/util.constants/issues"
	},
	"engines": {
		"node": ">=7.x"
	},
	"main": "./index.umd.min.js",
	"module": "./index.es.min.js",
	"types": "./index.d.ts",
	"files": [
		"index.*.min.js",
		"index.d.ts"
	],
	"devDependencies": {
		"@babel/cli": "^7.2.3",
		"@babel/core": "^7.3.4",
		"@babel/preset-env": "^7.3.4",
		"@babel/preset-typescript": "^7.3.3",
		"@babel/runtime": "^7.3.4",
		"@types/jest": "^24.0.9",
		"@types/node": "^11.9.5",
		"babel-minify": "^0.5.0",
		"babel-preset-minify": "^0.5.0",
		"coveralls": "^3.0.3",
		"debug": "^4.1.1",
		"jest": "^24.1.0",
		"prettier": "^1.16.4",
		"tslint": "^5.13.0",
		"tslint-config-prettier": "^1.18.0",
		"typescript": "^3.3.3333",
		"util.pkgwrap": "^0.0.101"
	},
	"scripts": {
		"all": "yarn install && yarn build && yarn test",
		"build": "yarn clean && yarn lint && yarn prettier && yarn build:umd && yarn build:es",
		"build:umd": "tsc -p . && minify index.js --out-file index.umd.min.js",
		"build:es": "babel index.js --out-file index.es.min.js",
		"clean": "pkgwrap --clean",
		"clean:dist": "yarn clean && rimraf node_modules/ yarn.lock",
		"coverage": "pkgwrap --coverage --jest",
		"lint": "pkgwrap --lint",
		"prepublishOnly": "yarn run all",
		"prettier": "prettier --write \"**/*.{ts,tsx,js,json,yaml,yml,css}\"",
		"test": "jest"
	}
}
